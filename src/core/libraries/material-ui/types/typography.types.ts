type Heading = {
  fontFamily: string;
  fontSize: string;
  lineHeight: string;
  fontWeight: string;
  letterSpacing: string;
};

type Typography = {
  fontFamily: string;
  h1: Heading;
  h2: Heading;
  h3: Heading;
  h4: Heading;
  h5: Heading;
  h6: Heading;
};

export default Typography;
