enum ActionTypes {
  TO_NEXT = 'TO_NEXT',
  TO_PREV = 'TO_PREV',
  TO_SLIDE = 'TO_SLIDE',
  SET_SLIDES = 'SET_SLIDES',
  SET_LOADING = 'SET_LOADING',
  SET_MODE = 'SET_MODE',
  SET_ERROR = 'SET_ERROR',
  RESTART = 'RESTART',
}

export default ActionTypes;
